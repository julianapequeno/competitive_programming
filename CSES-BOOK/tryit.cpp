#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
   // int a,b;
  //  cin >> a >> b;
   // cout << "Hi";

    //For reading a line
   // string s;
    //getline(cin,s);

    //unknown amount of data
    //while(cin >> a)
     //   cout << a;

    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
}